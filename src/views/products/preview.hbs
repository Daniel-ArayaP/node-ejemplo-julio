<div class="container p-4">
    <div class="row">
        <div class="col-md-12 mx-auto">
            <div class="card card-body">
                <div class="row">
                    <div class="col-4">
                        <img src="{{product.image.path}}" alt="{{product.name}}" class="img-fluid">
                    </div>
                    <div class="col-8">
                        <h3>{{product.name}}</h3>
                        <h2 class="display-4">{{product.price}}$</h2>
                        <p>{{product.description}}</p>
                        <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Ea nam commod.</p>
                        <a href="/cart/add/{{_id}}" class="btn btn-primary btn-lg">
                            <i class="fas fa-shopping-cart"></i> Add to Cart- Añadir al carrito
                        </a>
                    </div>
                </div>

            </div>
            <hr>
            <div class="card card-body">
                <p>Comments (30)</p>
            </div>
        </div>
    </div>
    